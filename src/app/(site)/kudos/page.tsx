import { ComingSoon } from "@/components/coming-soon";

/** Stub Sun* Kudos page — homepage links target this until it is built. */
export default function KudosPage() {
  return <ComingSoon titleKey="kudosPage.title" />;
}
